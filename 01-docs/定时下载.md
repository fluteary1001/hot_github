# 定时下载功能详细设计说明书

> 版本：1.0.0 | 日期：2026-05-22

---

## 1. 功能概述

### 1.1 功能名称

定时自动下载官方热点并生成 AI 分析报告 (Scheduled Trending Download & Analysis)

### 1.2 功能定位

基于"历史官方热点"功能，实现从采集到分析的完整自动化流程：定时采集热点 → 自动下载项目 → 凌晨自动生成分析报告。

### 1.3 核心价值

| 能力 | 描述 |
|---|---|---|
| 自动采集 | 晚上22:00自动采集每日/每周/每月热点 |
| 智能下载 | 采集后立即下载TOP N项目，已存在则更新 |
| AI分析 | 凌晨自动生成设计说明书、使用说明书、价值点分析HTML报告 |
| 失败重试 | 分析失败自动重试，最多3次，间隔1小时 |
| 报告链接 | 项目管理页面提供报告快捷访问 |

---

## 2. 系统架构

### 2.1 整体流程架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    定时任务调度器 (APScheduler)                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 22:00 采集   │  │ 采集后下载   │  │ 00:30 分析   │          │
│  │ 日/周/月热点 │  │ TOP N 项目   │  │ 生成HTML报告 │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
└─────────┼──────────────────┼──────────────────┼──────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    核心服务层                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │TrendingScraper│  │  Downloader │  │DocGenerator  │          │
│  │   爬取热点    │  │  git clone  │  │  AI分析生成  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    数据存储层 (SQLite)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │trending_history│ │   projects   │  │trending_reports│        │
│  │   热点历史    │  │   项目信息   │  │   分析报告   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 时序流程图

```
时间轴          任务                    数据库状态
────────────────────────────────────────────────────────────────
22:00:00    采集日热点                trending_history 新增记录
22:00:05    采集周热点（周一）         trending_history 新增记录
22:00:10    采集月热点（每月1日）      trending_history 新增记录
            ↓
22:00:15    查询本次采集的TOP N项目
            ↓
22:00:20    遍历项目：
              - 已下载？→ git pull 更新
              - 未下载？→ git clone 下载
            ↓
22:05:00    下载完成                  trending_history.downloaded=1
                                      projects 表更新
            ↓
00:30:00    检查所有下载任务完成
            ↓
00:30:05    串行执行AI分析：
              for project in projects:
                - 检查报告是否存在？跳过
                - 生成设计说明书HTML
                - 生成使用说明书HTML
                - 生成价值点分析HTML
                - 记录到 trending_reports
            ↓
02:00:00    全部完成或有失败          trending_reports.status更新
            ↓
03:00:00    重试失败任务（每小时）
```

---

## 3. 核心模块设计

### 3.1 定时任务调度器 (scheduler.py)

#### 3.1.1 任务配置

```python
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger

class TrendingScheduler:
    """热点采集与自动分析调度器"""

    def __init__(self):
        self.scheduler = BackgroundScheduler()
        self._setup_jobs()

    def _setup_jobs(self):
        """配置定时任务"""

        # 每天 22:00 采集日热点
        self.scheduler.add_job(
            self.collect_daily_trending,
            CronTrigger(hour=22, minute=0),
            id='daily_trending',
            name='每日热点采集'
        )

        # 每周一 22:00 采集周热点
        self.scheduler.add_job(
            self.collect_weekly_trending,
            CronTrigger(day_of_week='mon', hour=22, minute=0),
            id='weekly_trending',
            name='每周热点采集'
        )

        # 每月1日 22:00 采集月热点
        self.scheduler.add_job(
            self.collect_monthly_trending,
            CronTrigger(day=1, hour=22, minute=0),
            id='monthly_trending',
            name='每月热点采集'
        )

        # 凌晨 00:30 开始自动下载和分析
        self.scheduler.add_job(
            self.process_auto_download_and_analysis,
            CronTrigger(hour=0, minute=30),
            id='auto_analysis',
            name='自动下载与分析'
        )

        # 失败重试任务，每小时检查一次
        self.scheduler.add_job(
            self.retry_failed_analysis,
            CronTrigger(hour='*', minute=0),
            id='retry_analysis',
            name='失败重试'
        )
```

#### 3.1.2 采集任务实现

```python
def collect_daily_trending(self):
    """采集每日热点"""
    # 检查关闭信号
    if self.shutdown_event.is_set():
        return

    task_name = 'daily_trending'
    logger.info(f"开始执行: {task_name}")

    try:
        # 更新任务状态为运行中
        self.db.update_scheduler_task(task_name, 'running')

        # 创建爬虫实例
        scraper = TrendingScraper(
            self.settings.GITHUB_TOKEN,
            timeout=self.settings.GITHUB_API_TIMEOUT
        )

        # 获取今日周期值
        period_value = self.history_manager.get_today_period_value()
        limit = self.settings.TRENDING_OFFICIAL_LIMIT

        # 爬取热点数据
        results = scraper.get_trending(since='daily', limit=limit)
        saved = self.db.save_trending_history(results, 'daily', period_value)

        # 更新任务状态为完成
        self.db.update_scheduler_task(
            task_name, 'completed',
            result=f"保存了 {saved} 个项目",
            last_run_at=datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        )
        logger.info(f"每日热点采集完成: {saved} 个项目")

        # 采集后立即触发自动下载（如果配置启用）
        if self.settings.HISTORY_TRENDING_AUTO_DOWNLOAD:
            self._auto_download_trending(period_value)

    except Exception as e:
        logger.error(f"每日热点采集失败: {e}")
        self.db.update_scheduler_task(task_name, 'failed', result=str(e))
```

#### 3.1.3 自动下载实现

```python
def _auto_download_trending(self, period_value: str):
    """自动下载热点项目"""
    logger.info(f"开始自动下载热点项目: {period_value}")

    # 查询待下载的历史记录
    pending = self.db.get_pending_download_history(period_value)
    downloader = ProjectDownloader(self.settings.DOWNLOAD_PATH)
    download_path = Path(self.settings.DOWNLOAD_PATH)

    for item in pending:
        # 检查关闭信号
        if self.shutdown_event.is_set():
            break

        try:
            clone_url = item['clone_url']
            project_name = item['project_name']
            full_name = item['full_name']

            # 1. 先检查数据库中是否已存在该项目
            existing = self.db.get_project_by_name(full_name)
            if existing:
                # 已存在，执行更新并标记为已下载
                logger.info(f"项目已存在于数据库，执行更新: {project_name}")
                downloader.update_project(existing['local_path'])
                self.db.update_trending_downloaded(item['id'], existing['id'])
                logger.info(f"已标记为已下载: {project_name}")
                continue

            # 2. 检查本地目录是否已存在
            local_path = download_path / project_name
            if local_path.exists() and (local_path / '.git').exists():
                # 本地已存在有效仓库，直接添加到数据库并标记
                logger.info(f"本地目录已存在有效仓库: {local_path}")
                project_info = {
                    'name': project_name,
                    'full_name': full_name,
                    'html_url': item['html_url'],
                    'description': item['description'],
                    'stars': item['stars'],
                    'language': item['language'],
                    'clone_url': clone_url,
                    'local_path': str(local_path)
                }
                project_id = self.db.add_project(project_info)
                self.db.update_trending_downloaded(item['id'], project_id)
                logger.info(f"已添加到数据库并标记为已下载: {project_name}")
                continue

            # 3. 执行克隆下载
            logger.info(f"下载新项目: {project_name}")
            success, result = downloader.clone_project(clone_url, project_name)

            if success:
                # 保存到 projects 表
                project_info = {
                    'name': project_name,
                    'full_name': full_name,
                    'html_url': item['html_url'],
                    'description': item['description'],
                    'stars': item['stars'],
                    'language': item['language'],
                    'clone_url': clone_url,
                    'local_path': result  # result 是本地路径
                }
                project_id = self.db.add_project(project_info)
                self.db.update_trending_downloaded(item['id'], project_id)
                logger.info(f"下载完成: {project_name}")
            else:
                # 4. 克隆失败，再次检查本地是否实际已存在
                if local_path.exists() and (local_path / '.git').exists():
                    # 本地实际已存在，可能是之前下载成功的
                    logger.info(f"克隆失败但本地已存在有效仓库，标记为已下载: {project_name}")
                    project_info = {
                        'name': project_name,
                        'full_name': full_name,
                        'html_url': item['html_url'],
                        'description': item['description'],
                        'stars': item['stars'],
                        'language': item['language'],
                        'clone_url': clone_url,
                        'local_path': str(local_path)
                    }
                    project_id = self.db.add_project(project_info)
                    self.db.update_trending_downloaded(item['id'], project_id)
                else:
                    logger.error(f"下载失败: {project_name}, {result}")

        except Exception as e:
            logger.error(f"下载失败 {item['project_name']}: {e}")
            # 异常时也检查本地是否存在
            try:
                local_path = download_path / item['project_name']
                if local_path.exists() and (local_path / '.git').exists():
                    logger.info(f"异常但本地已存在，标记为已下载: {item['project_name']}")
                    project_info = {
                        'name': item['project_name'],
                        'full_name': item['full_name'],
                        'html_url': item['html_url'],
                        'description': item.get('description', ''),
                        'stars': item.get('stars', 0),
                        'language': item.get('language', ''),
                        'clone_url': item.get('clone_url', ''),
                        'local_path': str(local_path)
                    }
                    project_id = self.db.add_project(project_info)
                    self.db.update_trending_downloaded(item['id'], project_id)
            except Exception as e2:
                logger.error(f"异常恢复失败: {e2}")
```

#### 3.1.4 AI分析任务实现

```python
def process_auto_download_and_analysis(self):
    """处理自动下载和分析"""
    # 检查关闭信号
    if self.shutdown_event.is_set():
        return

    task_name = 'auto_analysis'
    logger.info(f"开始执行: {task_name}")

    try:
        self.db.update_scheduler_task(task_name, 'running')

        # 获取今日采集的热点
        today = self.history_manager.get_today_period_value()
        history = self.db.get_trending_history('daily', today)

        if not history:
            logger.info("今日无热点数据，跳过分析")
            self.db.update_scheduler_task(task_name, 'completed', result="无数据")
            return

        # 等待下载完成
        pending_downloads = [h for h in history if not h['downloaded']]
        if pending_downloads:
            logger.info(f"等待 {len(pending_downloads)} 个项目下载完成")
            self._auto_download_trending(today)

        # 执行 AI 分析
        if self.settings.HISTORY_TRENDING_AUTO_ANALYSIS:
            self._run_analysis_for_history(history)

        self.db.update_scheduler_task(
            task_name, 'completed',
            result=f"分析了 {len(history)} 个项目",
            last_run_at=datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        )

    except Exception as e:
        logger.error(f"自动分析失败: {e}")
        self.db.update_scheduler_task(task_name, 'failed', result=str(e))

def _run_analysis_for_history(self, history: list[dict]):
    """为历史热点执行 AI 分析"""
    generator = WebDocGenerator(
        download_path=self.settings.DOWNLOAD_PATH,
        api_key=self.settings.CLAUDE_API_KEY,
        model=self.settings.CLAUDE_MODEL,
        base_url=self.settings.CLAUDE_BASE_URL,
        max_tokens=self.settings.CLAUDE_MAX_TOKENS,
        cli_timeout=self.settings.CLAUDE_CLI_TIMEOUT,
        api_timeout=self.settings.CLAUDE_API_TIMEOUT,
        retry_count=self.settings.CLAUDE_RETRY_COUNT,
        analysis_method=self.settings.ANALYSIS_METHOD
    )

    failed_projects = []

    for item in history:
        # 检查关闭信号
        if self.shutdown_event.is_set():
            break

        if not item['downloaded'] or not item['project_id']:
            continue

        project = self.db.get_project(item['project_id'])
        if not project or not project['local_path']:
            continue

        # 检查是否已生成报告
        existing_report = self.db.get_trending_report_by_history(item['id'])
        if existing_report and existing_report['status'] == 'completed':
            logger.info(f"报告已存在，跳过: {item['project_name']}")
            continue

        logger.info(f"开始生成报告: {item['project_name']}")

        try:
            # 创建报告记录
            report_id = self.db.create_trending_report(
                item['id'], item['project_name'], item['project_id']
            )
            self.db.update_trending_report(report_id, {'status': 'processing'})

            # 生成报告
            local_path = project['local_path']
            project_name = item['project_name']

            results = generator.generate_all_docs(
                local_path,
                project_name,
                progress_callback=lambda k, s: None,
                shutdown_event=self.shutdown_event,
                generate_html=True,
                generate_pptx=False  # 只生成 HTML
            )

            # 更新报告路径
            docs_dir = Path(self.settings.DOWNLOAD_PATH) / "01-docs"
            self.db.update_trending_report(report_id, {
                'status': 'completed',
                'design_html_path': str(docs_dir / f"{project_name}-设计说明书.html"),
                'usage_html_path': str(docs_dir / f"{project_name}-使用说明书.html"),
                'value_html_path': str(docs_dir / f"{project_name}-价值点分析.html")
            })

            logger.info(f"报告生成完成: {item['project_name']}")

        except Exception as e:
            logger.error(f"报告生成失败 {item['project_name']}: {e}")
            failed_projects.append(item['project_name'])

            # 更新失败状态
            if existing_report:
                self.db.update_trending_report(existing_report['id'], {
                    'status': 'failed',
                    'error_message': str(e),
                    'retry_count': existing_report['retry_count'] + 1
                })
            else:
                report_id = self.db.create_trending_report(
                    item['id'], item['project_name'], item['project_id']
                )
                self.db.update_trending_report(report_id, {
                    'status': 'failed',
                    'error_message': str(e)
                })

    if failed_projects:
        logger.warning(f"失败的项目: {failed_projects}")
```

#### 3.1.5 失败重试机制

```python
def retry_failed_analysis(self):
    """重试失败的分析任务"""
    # 检查关闭信号
    if self.shutdown_event.is_set():
        return

    task_name = 'retry_analysis'
    max_retry = getattr(self.settings, 'HISTORY_TRENDING_MAX_RETRY', 3)

    # 查询失败且未超过重试次数的报告
    failed_reports = self.db.get_failed_reports_for_retry(max_retry)

    if not failed_reports:
        return

    logger.info(f"开始重试 {len(failed_reports)} 个失败任务")

    for report in failed_reports:
        # 检查关闭信号
        if self.shutdown_event.is_set():
            break

        try:
            # 获取对应的历史记录
            history = self.db.get_trending_history(
                report.get('period_type', 'daily'),
                report.get('period_value', '')
            )
            history_item = next(
                (h for h in history if h['id'] == report['history_id']), None
            )

            if not history_item:
                continue

            # 执行单个项目的分析
            self._run_single_analysis(history_item, report)

        except Exception as e:
            logger.error(f"重试失败 {report['project_name']}: {e}")

def _run_single_analysis(self, history_item: dict, report: dict):
    """执行单个项目的分析"""
    generator = WebDocGenerator(
        download_path=self.settings.DOWNLOAD_PATH,
        api_key=self.settings.CLAUDE_API_KEY,
        model=self.settings.CLAUDE_MODEL,
        base_url=self.settings.CLAUDE_BASE_URL,
        max_tokens=self.settings.CLAUDE_MAX_TOKENS,
        cli_timeout=self.settings.CLAUDE_CLI_TIMEOUT,
        api_timeout=self.settings.CLAUDE_API_TIMEOUT,
        retry_count=self.settings.CLAUDE_RETRY_COUNT,
        analysis_method=self.settings.ANALYSIS_METHOD
    )

    project = self.db.get_project(history_item['project_id'])
    if not project:
        return

    # 更新报告状态为处理中
    self.db.update_trending_report(report['id'], {'status': 'processing'})

    try:
        # 生成报告
        results = generator.generate_all_docs(
            project['local_path'],
            history_item['project_name'],
            progress_callback=lambda k, s: None,
            shutdown_event=self.shutdown_event,
            generate_html=True,
            generate_pptx=False  # 只生成 HTML
        )

        # 更新报告路径和状态
        docs_dir = Path(self.settings.DOWNLOAD_PATH) / "01-docs"
        self.db.update_trending_report(report['id'], {
            'status': 'completed',
            'design_html_path': str(docs_dir / f"{history_item['project_name']}-设计说明书.html"),
            'usage_html_path': str(docs_dir / f"{history_item['project_name']}-使用说明书.html"),
            'value_html_path': str(docs_dir / f"{history_item['project_name']}-价值点分析.html")
        })

    except Exception as e:
        # 更新失败状态和重试次数
        self.db.update_trending_report(report['id'], {
            'status': 'failed',
            'error_message': str(e),
            'retry_count': report['retry_count'] + 1
        })
        raise
```

---

## 4. 数据库设计

### 4.1 trending_history 表（热点历史记录）

```sql
CREATE TABLE IF NOT EXISTS trending_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    period_type TEXT NOT NULL,           -- 周期类型: 'daily'/'weekly'/'monthly'
    period_value TEXT NOT NULL,          -- 周期值: '2026-05-22'/'2026-W21'/'2026-05'
    year INTEGER NOT NULL,               -- 年份: 2026
    month INTEGER NOT NULL,              -- 月份: 1-12
    week INTEGER,                        -- 周数: 1-53 (仅weekly)
    day INTEGER,                         -- 日期: 1-31 (仅daily)
    rank INTEGER NOT NULL,               -- 排名: 1-N
    project_name TEXT NOT NULL,          -- 项目名称
    full_name TEXT NOT NULL,             -- 完整名称: owner/repo
    html_url TEXT,                       -- GitHub URL
    description TEXT,                    -- 项目描述
    stars INTEGER DEFAULT 0,             -- Star 数
    stars_change INTEGER DEFAULT 0,      -- Star 增长数
    language TEXT,                       -- 编程语言
    clone_url TEXT,                      -- Git clone URL
    collected_at TEXT NOT NULL,          -- 采集时间
    downloaded INTEGER DEFAULT 0,        -- 是否已下载: 0/1
    project_id INTEGER,                  -- 关联 projects 表 ID
    UNIQUE(period_type, period_value, full_name, rank)
);

CREATE INDEX idx_trending_history_period ON trending_history(period_type, period_value);
CREATE INDEX idx_trending_history_downloaded ON trending_history(downloaded);
```

### 4.2 trending_reports 表（AI分析报告记录）

```sql
CREATE TABLE IF NOT EXISTS trending_reports (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    history_id INTEGER NOT NULL,         -- 关联 trending_history.id
    project_id INTEGER,                  -- 关联 projects.id
    project_name TEXT NOT NULL,          -- 项目名称
    design_html_path TEXT,               -- 设计说明书 HTML 路径
    usage_html_path TEXT,                -- 使用说明书 HTML 路径
    value_html_path TEXT,                -- 价值点分析 HTML 路径
    status TEXT DEFAULT 'pending',       -- 状态: pending/processing/completed/failed
    error_message TEXT,                  -- 错误信息
    retry_count INTEGER DEFAULT 0,       -- 重试次数
    created_at TEXT NOT NULL,            -- 创建时间
    updated_at TEXT,                     -- 更新时间
    FOREIGN KEY (history_id) REFERENCES trending_history(id)
);

CREATE INDEX idx_trending_reports_status ON trending_reports(status);
CREATE INDEX idx_trending_reports_history ON trending_reports(history_id);
```

### 4.3 scheduler_tasks 表（定时任务状态）

```sql
CREATE TABLE IF NOT EXISTS scheduler_tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_name TEXT UNIQUE NOT NULL,      -- 任务名称
    last_run_at TEXT,                    -- 上次执行时间
    next_run_at TEXT,                    -- 下次执行时间
    status TEXT DEFAULT 'idle',          -- 状态: idle/running/completed/failed
    result TEXT,                         -- 执行结果
    created_at TEXT NOT NULL,
    updated_at TEXT
);
```

---

## 5. API 接口设计

### 5.1 手动触发采集

```python
# POST /api/history/collect
{
    "period_type": "daily"  # daily/weekly/monthly
}

# Response
{
    "success": true,
    "message": "采集任务已启动",
    "collected_count": 25,
    "period_value": "2026-05-22"
}
```

### 5.2 查询任务状态

```python
# GET /api/history/scheduler/status

# Response
{
    "tasks": [
        {
            "task_name": "daily_trending",
            "last_run_at": "2026-05-22 22:00:00",
            "next_run_at": "2026-05-23 22:00:00",
            "status": "completed",
            "result": "采集25个项目"
        },
        {
            "task_name": "auto_analysis",
            "last_run_at": "2026-05-22 00:30:00",
            "next_run_at": "2026-05-23 00:30:00",
            "status": "completed",
            "result": "分析完成20个项目，0个失败"
        }
    ]
}
```

### 5.3 查询报告详情

```python
# GET /api/history/reports?status=failed

# Response
{
    "reports": [
        {
            "id": 123,
            "project_name": "owner/repo",
            "status": "failed",
            "error_message": "API调用超时",
            "retry_count": 2,
            "created_at": "2026-05-22 01:00:00"
        }
    ]
}
```

---

## 6. 前端集成

### 6.1 ProjectsPage 增强报告链接

```vue
<!-- 在项目卡片中增加 -->
<div class="project-reports" v-if="project.reports">
  <el-divider />
  <div class="report-links">
    <span class="report-label">AI分析报告:</span>
    <el-link
      v-if="project.reports.design_html"
      :href="project.reports.design_html"
      target="_blank"
      type="primary"
    >
      设计说明书
    </el-link>
    <el-link
      v-if="project.reports.usage_html"
      :href="project.reports.usage_html"
      target="_blank"
      type="primary"
    >
      使用说明书
    </el-link>
    <el-link
      v-if="project.reports.value_html"
      :href="project.reports.value_html"
      target="_blank"
      type="primary"
    >
      价值分析
    </el-link>
  </div>
</div>
```

### 6.2 HistoryTrendingPage 展示报告

```vue
<!-- 在热点项目卡片中 -->
<div class="project-actions">
  <el-tag v-if="project.downloaded" type="success">已下载</el-tag>
  <div v-if="project.reports" class="report-links">
    <a :href="project.reports.design_html" target="_blank">设计说明书</a>
    <a :href="project.reports.usage_html" target="_blank">使用说明书</a>
    <a :href="project.reports.value_html" target="_blank">价值分析</a>
  </div>
</div>
```

---

## 7. 配置管理

### 7.1 新增配置项

```json
{
  "history_trending_enabled": true,
  "history_trending_auto_download": true,
  "history_trending_auto_analysis": true,
  "history_trending_collect_hour": 22,
  "history_trending_collect_minute": 0,
  "history_trending_analysis_start_hour": 0,
  "history_trending_analysis_start_minute": 30,
  "history_trending_max_retry": 3,
  "history_trending_retry_interval": 3600,
  "trending_official_limit": 25
}
```

### 7.2 配置项说明

| 配置项 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| history_trending_enabled | bool | true | 是否启用历史热点功能 |
| history_trending_auto_download | bool | true | 是否自动下载热点项目 |
| history_trending_auto_analysis | bool | true | 是否自动生成AI分析报告 |
| history_trending_collect_hour | int | 22 | 热点采集小时 |
| history_trending_collect_minute | int | 0 | 热点采集分钟 |
| history_trending_analysis_start_hour | int | 0 | 分析任务开始小时 |
| history_trending_analysis_start_minute | int | 30 | 分析任务开始分钟 |
| history_trending_max_retry | int | 3 | 分析失败最大重试次数 |
| history_trending_retry_interval | int | 3600 | 重试间隔（秒） |
| trending_official_limit | int | 25 | 采集TOP N项目数 |

---

## 8. 部署方案

### 8.1 Linux 服务器部署

```bash
# 1. 安装依赖
cd backend
pip install -r requirements.txt
pip install apscheduler

# 2. 配置 systemd 服务
sudo nano /etc/systemd/system/github-manager.service
```

### 8.2 systemd 服务配置

```ini
[Unit]
Description=GitHub Manager Web API
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/github_manager_web/backend
ExecStart=/usr/bin/python3 run.py
Restart=always
RestartSec=5
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

### 8.3 启动服务

```bash
sudo systemctl daemon-reload
sudo systemctl enable github-manager
sudo systemctl start github-manager
sudo systemctl status github-manager
```

---

## 9. 容错与监控

### 9.1 错误处理策略

| 错误类型 | 处理方式 |
|---|---|
| 热点采集失败 | 记录日志，更新任务状态为failed，不影响其他任务 |
| 项目下载失败 | 记录日志，检查本地是否已存在，继续下一个项目 |
| AI分析失败 | 记录到trending_reports，状态标记为failed，等待重试 |
| 重试耗尽 | 继续标记为failed，不再自动重试，可通过手动触发重试 |
| 关闭信号 | 所有任务检查shutdown_event，收到信号后立即退出 |

### 9.2 日志监控

```python
# 关键操作日志
logger.info("开始采集日热点")
logger.info(f"采集完成，共{len(projects)}个项目")
logger.info(f"开始下载项目 {project_name}")
logger.info(f"项目下载完成: {local_path}")
logger.info(f"开始分析项目: {project_name}")
logger.info(f"项目分析完成")
logger.error(f"项目 {project_name} 分析失败: {error}")
```

### 9.3 监控指标

| 指标 | 说明 |
|---|---|
| 采集成功率 | 成功采集次数 / 总采集次数 |
| 下载成功率 | 成功下载数 / 总项目数 |
| 分析成功率 | 成功分析数 / 总项目数 |
| 平均分析耗时 | 总分析时间 / 分析项目数 |
| 重试率 | 需要重试的任务数 / 总任务数 |

---

## 10. 性能优化

### 10.1 并发控制

- **采集任务**：单线程执行，避免GitHub API限流
- **下载任务**：顺序执行，避免磁盘IO竞争
- **AI分析**：单项目内三阶段并行（设计/使用/价值），项目间串行

### 10.2 资源限制

```python
# 配置参数
MAX_CONCURRENT_DOWNLOADS = 1  # 同时下载数
MAX_CONCURRENT_ANALYSIS = 3   # 单项目并行分析数
ANALYSIS_TIMEOUT = 600        # 单个文档超时时间（秒）
```

### 10.3 数据清理

```python
# 定期清理过期数据（可选）
def cleanup_old_data():
    """清理超过6个月的历史数据"""
    cutoff_date = datetime.now() - timedelta(days=180)

    # 删除过期热点记录
    db.execute("""
        DELETE FROM trending_history
        WHERE datetime(collected_at) < ?
    """, (cutoff_date.isoformat(),))

    # 删除过期报告记录
    db.execute("""
        DELETE FROM trending_reports
        WHERE datetime(created_at) < ?
    """, (cutoff_date.isoformat(),))
```

---

## 11. 测试验证

### 11.1 单元测试

```python
def test_collect_daily_trending():
    """测试日热点采集"""
    scheduler = TrendingScheduler()
    scheduler.collect_daily_trending()

    # 验证数据库记录
    records = db.get_trending_history('daily', '2026-05-22')
    assert len(records) > 0
    assert records[0]['period_type'] == 'daily'

def test_auto_download():
    """测试自动下载"""
    scheduler = TrendingScheduler()
    scheduler.auto_download_projects('daily', '2026-05-22')

    # 验证下载状态
    records = db.get_trending_history('daily', '2026-05-22')
    for record in records:
        assert record['downloaded'] == 1

def test_ai_analysis():
    """测试AI分析"""
    scheduler = TrendingScheduler()
    scheduler.process_auto_download_and_analysis()

    # 验证报告生成
    reports = db.get_trending_reports(status='completed')
    assert len(reports) > 0
```

### 11.2 集成测试

```bash
# 手动触发采集
curl -X POST http://localhost:8000/api/history/collect \
  -H "Content-Type: application/json" \
  -d '{"period_type": "daily"}'

# 查询任务状态
curl http://localhost:8000/api/history/scheduler/status

# 查询报告
curl http://localhost:8000/api/history/reports?status=failed
```

---

## 12. 注意事项

### 12.1 时区问题

- 所有时间使用服务器本地时区
- 定时任务配置基于服务器时区
- 数据库存储时间使用ISO格式字符串

### 12.2 GitHub API限流

- 未认证请求：60次/小时
- 认证请求：5000次/小时
- 建议：配置GitHub Token，避免限流

### 12.3 AI API成本

- 单个项目生成3个HTML报告
- 预估Token消耗：~15000 tokens/项目
- 建议：监控API调用量，设置预算上限

### 12.4 磁盘空间

- 单个项目平均大小：~50MB
- 25个项目：~1.25GB
- 报告文件：~500KB/项目
- 建议：定期清理旧项目，监控磁盘使用

---

## 13. 扩展规划

### 13.1 短期优化

- [ ] 支持自定义采集时间
- [ ] 支持手动触发单个项目分析
- [ ] 增加分析进度实时推送
- [ ] 支持PDF格式报告生成

### 13.2 长期规划

- [ ] 支持多语言报告
- [ ] 支持自定义分析模板
- [ ] 增加项目相似度分析
- [ ] 支持趋势预测分析

---

## 14. 参考资料

- [APScheduler 官方文档](https://apscheduler.readthedocs.io/)
- [GitHub Trending 页面](https://github.com/trending)
- [Claude API 文档](https://docs.anthropic.com/)
- [历史官方热点功能设计说明书](./历史官方热点功能设计说明书.md)
- [AI分析流程-后端详解](./AI分析流程-后端.md)
